import {writable} from 'svelte/store'
export const calendarGridHeight = writable(0);
export const headerHeight = writable(0);
export const dayDetailListElementHeight = writable(0);
export const dayDetailListRemainingHeight = writable(0)
